packages:
  - "apps/*"
  - "packages/*"

# Allow these packages to run install/build scripts (pnpm blocks them by default).
allowBuilds:
  "@prisma/client": true
  "@prisma/engines": true
  prisma: true
  esbuild: true
