{
  "framework": "nextjs",
  "buildCommand": "prisma generate --schema=../../packages/database/prisma/schema.prisma && next build",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "feat/finish-mvp-buildable": false
    }
  }
}
